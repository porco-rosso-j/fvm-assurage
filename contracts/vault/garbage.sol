
/*

    function addProtectionVault(address _pvault) public onlyOwner {
        require(pvaultIndex[_pvault] == 0, "Address has already been registered");
        pvaults.push(_pvault);    
    }

    mapping (address => uint) public pvaultIndex;
    address[] public pvaults;

*/